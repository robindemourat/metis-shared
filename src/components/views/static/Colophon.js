import React from 'react';

export default ({
  contents
}) => (
  <section className="colophon break-before break-after metis-StaticMontageSection">
    <div>
      {contents}
    </div>
  </section>
);
